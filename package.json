{
  "name": "bilateral-escrow",
  "version": "0.1.0",
  "private": true,
  "description": "Bilateral escrow on Starknet - Cairo contract with TypeScript tests, all via Docker",
  "scripts": {
    "build": "docker compose run --rm cairo scarb build",
    "fmt": "docker compose run --rm cairo scarb fmt",
    "clean": "docker compose run --rm cairo scarb clean",
    "shell": "docker compose run --rm cairo bash",
    "snforge": "docker compose run --rm cairo snforge test",
    "up": "docker compose up -d devnet",
    "accounts": "npm run up && node scripts/devnet.mjs accounts",
    "balance": "node scripts/devnet.mjs balance",
    "mint": "node scripts/devnet.mjs mint",
    "status": "node scripts/devnet.mjs status",
    "ui": "start http://127.0.0.1:5050/ui",
    "logs": "docker compose logs -f devnet",
    "reset": "docker compose rm -sf devnet",
    "down": "docker compose down -v",
    "test": "npm run up && docker compose run --rm tests npx vitest run",
    "test:watch": "npm run up && docker compose run --rm tests npx vitest",
    "test:local": "npm run up && npm --prefix tests run test --",
    "typecheck": "npm --prefix tests run typecheck",
    "deploy": "npm run up && docker compose run --rm tests npm run deploy",
    "deploy:local": "npm run up && npm --prefix tests run deploy --",
    "setup": "npm run build && npm run test"
  },
  "engines": {
    "node": ">=22"
  }
}
